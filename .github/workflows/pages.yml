name: Deploy dossier to GitHub Pages

on:
  push:
    branches:
      - claude/wizardly-mayer-ocjx0y
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configure Pages (auto-enables if permitted)
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload site artifact (repo root; index.html redirects to /site)
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
