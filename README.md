# JR Tech Solution — Static Site Export

A static, WordPress-free recreation of **https://jrtech.com.my/** (JR Tech Solution —
Commercial Kitchen Solution Provider), crawled and exported to plain HTML + assets.

## Live site & deployment

The root `index.html` is a portal page that links to every site hosted in this repo
(currently the `jrtech.com.my/` export). Pushing to **`main`** triggers
`.github/workflows/deploy.yml`, which publishes the entire repository — the portal page
and every site folder — to **GitHub Pages**. You can also run the deploy manually from the
**Actions** tab via **"Run workflow"** (works from any branch).

> Fallback: if Pages isn't auto-enabled by the workflow, set
> **Settings → Pages → Source** to **"GitHub Actions"**.

## Why

The live site is a WordPress install that currently prints PHP warnings on every page
(duplicate `define()`s in `wp-config.php` and a WPBakery/`js_composer` plugin that is
incompatible with the server's PHP 8 version). This export captures the rendered site as
self-contained static files that open directly in a browser — no PHP, database, or
WordPress required. The PHP `Warning:` lines that the broken site prepended to every page
were stripped during export.

## How to view

Open **`jrtech.com.my/index.html`** in any browser. Internal links were rewritten to work
offline, so you can click through the whole site locally.

## Contents

| Path | Description |
|------|-------------|
| `jrtech.com.my/index.html` | Home page (entry point) |
| `jrtech.com.my/product-category/` | WooCommerce product category pages |
| `jrtech.com.my/<product>/` | Individual product pages |
| `jrtech.com.my/about-us/`, `/contact-us/`, `/news/` … | Content pages |
| `jrtech.com.my/wp-content/uploads/` | All images & media |
| `jrtech.com.my/wp-content/{themes,plugins}/` | CSS & JS needed for layout |

## Export stats

- **HTML pages:** 28
- **Images:** 337
- **Total size:** 19M
- **Source:** crawl of https://jrtech.com.my/ (sitemap-seeded, full mirror)

> Generated by an automated crawl. External resources (Google Fonts, etc.) still load from
> their original CDNs; all first-party content is local.
